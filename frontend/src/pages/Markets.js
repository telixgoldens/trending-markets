import React from "react";
import Dashboard from "./Dashboard";

export default function Markets() {
  // For now reuse Dashboard view (list). You can add filters here later.
  return <Dashboard />;
}
